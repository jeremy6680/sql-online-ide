# Server port (default: 3001)
PORT=3001

# ─── Authentication ──────────────────────────────────────────────────────────
#
# Optional. If not set, the app is accessible to everyone and the
# "Sign in" button is hidden.
#
# Passwords are plaintext here — they are hashed with bcrypt at server startup.
#
# Two supported formats:
#
# Simple (recommended for Coolify / Docker env vars):
# AUTH_USERS=admin:changeme
# AUTH_USERS=alice:passA,bob:passB
#
# JSON array:
# AUTH_USERS=[{"username":"admin","password":"changeme"}]
#
AUTH_USERS=

# Secret used to sign JWT tokens. Use a long random string in production.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-me-in-production
